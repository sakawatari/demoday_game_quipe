<h1>GENRE</h1>

<% @genres.each do |g| %>
    <%= search_form_for(@q, url: games_path, method: :get) do |f| %>
        <%= f.hidden_field :genre_id_eq, value: g.id %>
        <%= f.submit g.genre, class: "btn" %>
    <% end %>
<% end %>  